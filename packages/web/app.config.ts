// TODO how to merge with server configs, probably move to nuxt.config.ts
export default defineAppConfig({
    title: 'InitFS',
    status: {
        maintenance: false,
        version: '',
    },
});
